{% extends "layout.twig" %}
{% block content %}
    <div id="content" class="container pt-3 pt-md-0">
        <h1 class="mt-3">Simple CMS</h1>

        <div class="card mt-3">
            <div class="card-header d-flex align-items-center justify-content-between">
                <h5 class="mb-0">Contact Details</h5>
                <a href="/cms/create/contact-detail" class="btn btn-sm btn-success">
                    Add New
                </a>
            </div>
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Title</th>
                            <th>Link</th>
                            <th>Text</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for contact in contactDetails %}
                            <tr>
                                <td>{{ contact.id }}</td>
                                <td>{{ contact.title }}</td>
                                <td><a href="{{ contact.link }}">{{ contact.link }}</a></td>
                                <td>{{ contact.text }}</td>
                                <td class="d-flex align-items-center gap-2">
                                    <!-- Example actions: Edit, Delete -->
                                    <a href="#" class="btn btn-sm btn-secondary">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form action="/cms/delete/contact-detail" method="post">
                                        <input type="hidden" name="id" value="{{ contact.id }}">
                                        <button class="btn btn-sm btn-danger">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card mt-5">
            <div class="card-header d-flex align-items-center justify-content-between">
                <h5 class="mb-0">Employment History</h5>
                <a href="/cms/create/employment-history" class="btn btn-sm btn-success">
                    Add New
                </a>
            </div>
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Title</th>
                            <th>Link</th>
                            <th>Text</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for job in employmentHistory %}
                            <tr>
                                <td>{{ job.id }}</td>
                                <td>{{ job.employer }}</td>
                                <td>{{ job.position }}</td>
                                <td>{{ job.time }}</td>
                                <td class="d-flex align-items-center gap-2">
                                    <!-- Example actions: Edit, Delete -->
                                    <a href="#" class="btn btn-sm btn-secondary">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form action="/cms/delete/employment-history" method="post">
                                        <input type="hidden" name="id" value="{{ job.id }}">
                                        <button class="btn btn-sm btn-danger">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        
    </div>
{% endblock %}